import 'package:flutter/material.dart';

class AppColors {
  // Common
  static const transparent = Colors.transparent;
  static const white = Colors.white;
  static const black = Colors.black;

  // --- LIGHT THEME COLORS ---
  static const Color lightPrimary = Color(0xFF0D47A1);         // Dark Blue
  static const Color lightOnPrimary = Colors.white;
  static const Color lightSecondary = Color(0xFF009688);       // Teal
  static const Color lightOnSecondary = Colors.white;
  static const Color lightBackground = Color(0xFFF9F9F9);      // Off white
  static const Color lightSurface = Color(0xFFFFFFFF);         // Card, sheets
  static const Color lightOnBackground = Color(0xFF212121);    // Dark text
  static const Color lightOnSurface = Color(0xFF333333);       // Medium text
  static const Color lightDisabled = Color(0xFFBDBDBD);        // Disabled
  static const Color lightDivider = Color(0xFFE0E0E0);         // Divider
  static const Color lightError = Color(0xFFD32F2F);           // Red
  static const Color lightSuccess = Color(0xFF4CAF50);         // Green
  static const Color lightWarning = Color(0xFFFF9800);         // Orange
  static const Color lightInfo = Color(0xFF2196F3);            // Blue

  // --- DARK THEME COLORS ---
  static const Color darkPrimary = Color(0xFF90CAF9);          // Light Blue
  static const Color darkOnPrimary = Colors.black;
  static const Color darkSecondary = Color(0xFF80CBC4);        // Light Teal
  static const Color darkOnSecondary = Colors.black;
  static const Color darkBackground = Color(0xFF121212);       // Pure Dark
  static const Color darkSurface = Color(0xFF1E1E1E);           // Card BG
  static const Color darkOnBackground = Color(0xFFE0E0E0);     // Light text
  static const Color darkOnSurface = Color(0xFFCCCCCC);        // Light-mid text
  static const Color darkDisabled = Color(0xFF616161);         // Disabled
  static const Color darkDivider = Color(0xFF424242);          // Divider
  static const Color darkError = Color(0xFFEF5350);            // Red
  static const Color darkSuccess = Color(0xFF66BB6A);          // Green
  static const Color darkWarning = Color(0xFFFFB74D);          // Orange
  static const Color darkInfo = Color(0xFF64B5F6);             // Blue
}
